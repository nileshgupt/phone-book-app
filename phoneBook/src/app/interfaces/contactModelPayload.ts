
/**This model contains data when new contact is to be created */
export interface ContactModelPayload {
    name: string;
    phoneNumber: string[];
    email: string[],
    dateOfBirth: string,

}
